import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const Client = () => {
  return (
    <SafeAreaView>
      <Text>Client</Text>
    </SafeAreaView>
  );
};

export default Client;
